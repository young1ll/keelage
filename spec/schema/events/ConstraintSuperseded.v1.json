{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/young1ll/keelage/spec/schema/events/ConstraintSuperseded.v1.json",
  "$ref": "#/$defs/ConstraintSuperseded",
  "$defs": {
    "ActorRef": {
      "properties": {
        "kind": {
          "type": "string"
        },
        "id": {
          "type": "string"
        },
        "key": {
          "type": "string",
          "description": "Key is the signing key fingerprint."
        },
        "owner": {
          "type": "string",
          "description": "Owner is the responsible human for an agent instance. Required for agents."
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "kind",
        "id"
      ],
      "description": "ActorRef identifies who acted."
    },
    "ConstraintSuperseded": {
      "properties": {
        "id": {
          "type": "string"
        },
        "by": {
          "type": "string"
        },
        "at": {
          "type": "string",
          "format": "date-time"
        },
        "actor": {
          "$ref": "#/$defs/ActorRef"
        }
      },
      "additionalProperties": false,
      "type": "object",
      "required": [
        "id",
        "by",
        "at",
        "actor"
      ],
      "description": "ConstraintSuperseded retires in favour of another."
    }
  }
}

// Package app holds the use cases: the command pipeline
// (authorize → load → context → decide → append → publish → respond)
// and read-side queries over projections. It depends on core and port only.
package app
